# 안드로이드에서 맥 Claude Code SSH 접속 세팅 가이드

## 준비물
- 맥 (SSH 서버)
- 안드로이드 폰 (클라이언트)
- 동일한 Google 계정 (Tailscale용)

---

## PART 1: 맥에서 SSH 켜기

1. 화면 좌측 상단 **애플 로고** 클릭
2. **"시스템 설정"** 클릭
3. 왼쪽 목록에서 **"일반"** 클릭
4. **"공유"** 클릭
5. **"원격 로그인"** 오른쪽 토글 스위치를 **켜기**
6. "원격 사용자의 전체 디스크 접근 허용" 팝업 뜨면 **"허용"** 클릭
7. `사용자 이름@컴퓨터이름` 이 보이면 성공

## PART 1-B: 잠자기 방지

1. 화면 좌측 상단 **애플 로고** 클릭
2. **"시스템 설정"** 클릭
3. 왼쪽 목록에서 **"에너지 절약"** (또는 **"배터리"** → **"옵션"**) 클릭
4. **"디스플레이가 꺼져 있을 때 자동으로 잠자기 방지"** 체크
5. 전원 어댑터 연결 상태에서 잠자기를 **"안 함"**으로 설정
6. 설정 창 닫기

## PART 1-C: 맥 사용자 이름 확인

1. **터미널** 열기 (Spotlight에서 `터미널` 검색)
2. `whoami` 입력 후 엔터
3. 나오는 텍스트가 **사용자 이름** → 메모해두기

---

## PART 2: 맥에 Tailscale 설치

1. 맥에서 **Safari** (또는 Chrome) 열기
2. 주소창에 **`tailscale.com/download/mac`** 입력 후 엔터
3. **"Download from Mac App Store"** 버튼 클릭
4. App Store가 열리면 **"받기"** 버튼 클릭
5. 설치 완료되면 **"열기"** 클릭
6. 상단 메뉴바에 Tailscale 아이콘이 나타남 → 클릭
7. **"Log in"** 클릭
8. 브라우저가 열리면 **Google 계정**으로 로그인
9. "Connect your device?" 화면에서 **"Connect"** 클릭
10. 메뉴바 Tailscale 아이콘 클릭 → **Connected** 상태면 성공

---

## PART 3: 안드로이드에 Tailscale 설치

1. 폰에서 **Play Store** 열기
2. 검색창에 **"Tailscale"** 입력
3. **Tailscale Inc.** 개발자의 앱 → **"설치"** 터치
4. 설치 완료 → **"열기"** 터치
5. **"Sign in with Google"** 터치 → **맥에서 쓴 것과 같은 계정**으로 로그인
6. VPN 연결 허용 팝업 → **"확인"** 터치
7. 화면에 맥 이름이 보이면 성공 (예: `users-macbook-pro`)
8. 맥 이름 옆에 **IP 주소** (100.x.x.x) → 메모해두기

---

## PART 4: 안드로이드에 Termius 설치 + 맥 접속

1. 폰에서 **Play Store** 열기
2. 검색창에 **"Termius"** 입력
3. **Termius - SSH and SFTP client** → **"설치"** 터치
4. 설치 완료 → **"열기"** 터치
5. 계정 생성 화면 → **"Skip"** (또는 계정 만들어도 됨)
6. 하단 **"Hosts"** 탭 터치
7. 우측 상단 **"+"** 버튼 터치
8. **"New Host"** 터치
9. 아래 정보 입력:
   - **Alias**: `내 맥` (아무 이름)
   - **Hostname**: PART 3의 8번에서 메모한 **100.x.x.x** IP
   - **Port**: **22**
   - **Username**: PART 1-C의 3번에서 메모한 **사용자 이름**
   - **Password**: **맥 로그인 비밀번호**
10. 우측 상단 **체크(✓)** 또는 **"Save"** 터치
11. 저장된 **"내 맥"** 호스트 터치
12. "Trust this host?" 팝업 → **"Continue"** 터치
13. 터미널 화면이 뜨고 `user@mac ~ %` 프롬프트가 보이면 **성공**
14. **`claude`** 입력 후 엔터 → Claude Code 실행

---

## 완료 확인 체크리스트

- [ ] 맥: 원격 로그인 켜짐
- [ ] 맥: 잠자기 방지 설정
- [ ] 맥: Tailscale 연결됨
- [ ] 폰: Tailscale 같은 계정 연결됨
- [ ] 폰: Termius에서 맥 접속 성공
- [ ] 폰: `claude` 입력하면 Claude Code 실행됨
